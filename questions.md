# Questions:

1. How to reference data from one js file to another?

2. general approach: store 'isVisible' parameter as location object property,

3. Store marker (= google marker class) as location object property(method). Will it appear on map automatically once this property get declared?

4. Use condition 'visible' data bind in html file to display locations when filter updates

5. Use 'isVisible' prop to filter markers, that will be shown on the map